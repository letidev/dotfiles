git:
  diffContextSize: 3
  diffRenderers:
    - colorArg: always
      command: delta --paging=never --line-numbers --syntax-theme="gruvbox-dark"
    - colorArg: always
      command: delta --paging=never --line-numbers --side-by-side --syntax-theme="gruvbox-dark"
  overrideGpg: true
