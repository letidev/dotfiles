git:
  diffContextSize: 20
  pagers:
    - colorArg: always
      pager: delta --paging=never --line-numbers --side-by-side --syntax-theme="gruvbox-dark"
    - colorArg: always
      pager: delta --paging=never --line-numbers --syntax-theme="gruvbox-dark"
